# Server-only. The browser never talks to this directly — every call is
# proxied through the Next route handlers under /api.
API_BASE_URL="https://ph-b7-assignemnt-4.vercel.app/api/v1"
AUTH_COOKIE_NAME="gearup_token"

# Exposed to the browser.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_YOUR_PUBLISHABLE_KEY_HERE"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
